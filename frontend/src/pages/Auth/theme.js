import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f1a7b0", // Primary color
    },
    secondary: {
      main: "#f3d6da", // Secondary color
    },
    background: {
      default: "#fcf2f3", // Background color
    },
    text: {
      primary: "#120603", // Text color
    },
    accent: {
      main: "#f4bfc6", // Accent color
    },
  },
});

export default theme;
